{
  "name": "gobeyondjs",
  "version": "1.0.0",
  "description": "A journey to find a replacement to node modules in the backend in modern web development",
  "main": "app.js",
  "scripts": {
    "dev": "node app.js && npx tailwindcss -i ./src/input.css -o ./src/output.css --watch",
    "tailwind": "npx tailwindcss -i ./src/input.css -o ./public/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "HTMX",
    "Go"
  ],
  "author": "github.com/ydkulks",
  "license": "MIT",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "htmx.org": "^1.9.10"
  }
}
